import { css } from '@emotion/react';
import React from 'react';

interface HeaderProps {
}

export const Header = ({
    
}: HeaderProps) =>{
    return(
        <header css={{
            width: "100vw"
        }}>
            logo
        </header>
    )
}