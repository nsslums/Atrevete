interface btnProps {

}

export const Button = ({

}:btnProps) => {
    return (
        <a css={{
            borderRadius: '10px',
            '&:hover': {

            }
        }} href="">link</a>
    );
};