import { css } from '@emotion/react';
import React from 'react';

interface FooterProps {
}

export const Footer = ({
    
}: FooterProps) =>{
    return(
        <footer css={{
            width: "100vw"
        }}>
            logo
        </footer>
    )
}